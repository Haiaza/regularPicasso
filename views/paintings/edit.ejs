<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= painting.name %>
    </title>
</head>

<body>
<%- include('../partials/_navbar.ejs') %>
    <nav>
        <h1>Edit <%= painting.name %></h1>
        <form action="/users/<%= user._id %>/paintings/<%= painting._id %>?_method=PUT" method="POST">
        <label for="name">Painting Name:</label>
        <input type="text" name="name" id="name" value="<%= painting.name %>" required>
        <label for="description">Description:</label>
        <input type="text" name="description" id="description" value="<%= painting.description %>" required>
        <button type="submit">Update Painting</button>
    </form>

    <from action="/users/<%= user._id %>/paintings/<%= painting._id %>">
        <button type="submit">Back</button>
    </from>
    </nav>

</body>

</html>